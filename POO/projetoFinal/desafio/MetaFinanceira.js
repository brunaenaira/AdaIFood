export default class MetaFinanceira {
    constructor(descricao, valorMeta, progresso) {
        this.descricao = descricao
        this.valorMeta = valorMeta
        this.progresso = progresso
    }
}